<!DOCTYPE html>
<html>
  <head>
    <title>Databasium</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
    <%= turbo_include_tags %>

    <%= stylesheet_link_tag "databasium", "data-turbo-track": Rails.env.production? ? "reload" : "" %>

  </head>
  <body>

  <%= yield %>
  
  </body>
</html>
